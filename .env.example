# ------------------------------------------------------------------
# Copy this file to `.env` and fill in values.
# Do NOT commit `.env` to source control.
# ------------------------------------------------------------------

# Environment / runtime
APP_ENV=dev
LOG_LEVEL=INFO

# Authentication / service access
SERVICE_BASE_URL=https://internal-reporting-service.example.local
SERVICE_API_KEY=replace_me

# Delivery / output config
OUTPUT_PATH=/workspace/output
DELIVERY_TARGET=s3://internal-bucket/some/prefix
